# TODO

haltija's work lives on the virta board, not in this file. In a session, `virta brief` shows
what's in progress, ready and new (the hooks in `.claude/settings.json` run it for you), and
`virta ls "project:haltija status:ready"` lists what's been given the go-ahead. The board itself
is at https://virta.tosijs.net/host/#?virta.scope=haltija. File new work with `virta create`.

This file used to be the backlog. It was imported onto the board on 2026-09-26, and each card
links back to its exact line in the last version of this file (commit `6b71331`), so nothing was
lost. The design records and release history that were in it now live in `docs/DECISIONS.md`.

Keep this file free of list items: the importer reads every one as a task, and two lists drift
apart.
